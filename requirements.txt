# Subtitle Studio requirements
# Tested environment for XTTS v2:
#   Python 3.12
#   NVIDIA CUDA PyTorch wheels (CUDA 12.6 example)
#
# Install PyTorch FIRST with the command appropriate for your GPU/CPU:
#   pip install torch torchvision torchaudio torchcodec --index-url https://download.pytorch.org/whl/cu126
# Then install this file:
#   python -m pip install -r requirements.txt

# ---- Core dependencies ----
faster-whisper>=1.1.0
customtkinter>=5.2.2
deep-translator>=1.11.4
imageio-ffmpeg>=0.5.1
numpy>=1.26

# ---- Offline translation: NLLB-200 ----
# PyTorch packages are installed separately above.
transformers>=4.57,<5
sentencepiece>=0.2.0

# ---- Translated audio: Edge TTS ----
edge-tts>=6.1.12

# ---- Translated audio: XTTS v2 / Coqui ----
# The package is installed as coqui-tts but imported as TTS.api.
# Recent coqui-tts releases do not install PyTorch automatically.
coqui-tts>=0.27.4

# Compatibility note:
# Do not pin Transformers to 4.46.2 for the current coqui-tts setup.
# The installed Coqui XTTS code imports isin_mps_friendly from
# transformers.pytorch_utils, so use a recent Transformers 4.x release.
